# 管理者指示

現在有効な指示だけを書く。

## CURRENT
- 2026-08-29 PR可視化移行: 変更を伴う `IN_PROGRESS` タスクは、初回pushと同じ作業ターン内にDraft PRを作り、`01_アクティブタスク.md` へブランチ名とPR番号を登録する。
- 既存の `INT-010-TECH` は次回作業開始時に現在のブランチをpushし、Issue #10を関連付けたDraft PRを作る。PRを作れない場合は、ブランチ名、未pushの理由、退避または引継ぎ方法をIssue #10へ返信する。
- PRのない作業ブランチは正式進捗・依存解除・完了判定の根拠にしない。

## 巡回方針
- マネージャー: 1時間ごと
- 他担当: 自分の巡回タイミング時のみ
- 変更がなければ即終了
