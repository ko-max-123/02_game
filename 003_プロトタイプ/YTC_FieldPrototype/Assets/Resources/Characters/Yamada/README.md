# 山田モデル配置先

Editorセットアップが、デザインマネージャーの正式OBJ `yamada_k1_prototype_v1.obj` を `Yamada.obj` としてこのフォルダへ自動同期します。同期ファイルは生成物のためGit管理対象外です。

実行時のResourcesパスは `Characters/Yamada/Yamada` です。正式ソースが見つからない場合は、動作検証用カプセルが自動表示されます。
