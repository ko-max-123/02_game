# デモフィールド配置先

Editorセットアップが、デザインマネージャーの正式OBJ `central_belt_stage01_demo_field_v1.obj` を `DemoField.obj` としてこのフォルダへ自動同期します。同期ファイルは生成物のためGit管理対象外です。

実行時のResourcesパスは `Environment/DemoField/DemoField` です。正式ソースが見つからない場合は、移動・ジャンプ検証用の簡易フィールドが自動生成されます。
